import { ReplaceMoneyPipe } from './replace-money.pipe';

describe('ReplaceMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
